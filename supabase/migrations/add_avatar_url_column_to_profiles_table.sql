/*
  # Add avatar_url column to profiles table

  1. Changes
    - Add `avatar_url` column to `profiles` table
  2. Security
    - No security changes.
*/

-- ALTER TABLE profiles
-- ADD COLUMN avatar_url TEXT;